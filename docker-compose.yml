version: '3.8'

services:
  db:
    image: postgres:16
    container_name: zonein-db
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - zonein-db-data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  zonein-db-data: